# Third-party notices

This project is licensed under Apache-2.0. Dependencies retain their own licenses.

`Cargo.lock` and `package-lock.json` identify resolved packages but are not complete license inventories. Before distributing a compiled binary, generate and review a complete dependency-license and notice bundle for that exact lockfile state.

## libmdbx

The vendored libmdbx C library is Apache-2.0. Its upstream notice, as shipped by `mdbx-sys` 13.11.0, is reproduced verbatim in [NOTICE](NOTICE).

The `libmdbx` and `mdbx-sys` Rust bindings are MPL-2.0 and are used unmodified from crates.io.

## PowerSync

PowerSync is a product of Journey Mobile, Inc. This repository is not affiliated with, endorsed by, or supported by PowerSync or Journey Mobile, Inc. The benchmark runs the official `journeyapps/powersync-service` image and PowerSync npm packages under their own licenses and terms.
